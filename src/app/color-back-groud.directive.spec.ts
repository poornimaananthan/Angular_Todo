import { ColorBackGroudDirective } from './color-back-groud.directive';

describe('ColorBackGroudDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBackGroudDirective();
    expect(directive).toBeTruthy();
  });
});
